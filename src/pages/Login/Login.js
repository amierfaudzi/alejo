import React from 'react';
import './Login.scss';

export default function Login() {
    return (
        <div className="login">
            <div className="sign-in">
                <form>
                    <h4>Sign in</h4>
                    <button>Sign In</button>
                </form>
            </div>
        </div>
    )
}


// Signup will add open up a modal with the window and while